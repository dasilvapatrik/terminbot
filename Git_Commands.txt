status = git status

git anlegen = git init

untracket files add = git add filename

git log = history

changes to be committed = 

git help commit
commit erzeuge = git commit
git commit -a -m "hallo textdatei2 hinzugefuegt"

git help commit

update auf github:
…or push an existing repository from the command line
git remote add origin https://github.com/dasilvapatrik/terminbot.git
git push -u origin master